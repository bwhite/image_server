Brandyn A. White <bwhite@dappervision.com>

Requires: PIL and web.py

Usage:
- Go into a directory with lots of images
- python -m image_server --port=<port>


Usage (for selecting images to move to a folder):
- Go into a directory with lots of images
- Choose a directory into which to move selected images
- python -m image_server --port=<port> --movedir=<move dir>